import WritePageClient from '@/components/WritePageClient';

export const metadata = {
  title: 'Write for Us — Game Review Magazine',
  description:
    'Submit a pitch to Game Review Magazine. We publish independent critical voices with something real to say about games.',
};

export default function WritePage() {
  return <WritePageClient />;
}
